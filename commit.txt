feat(approval): implement generic event-driven approval system and metadata pattern

- Implement Generic Approval Module with polymorphic relations (referenceType/referenceId).
- Introduce `AuditMetadata` pattern in Core Domain to maintain DDD purity while supporting Optimistic Locking (versioning).
- Refactor `News` Aggregate to enforce approval workflow: DRAFT -> PENDING_APPROVAL -> PUBLISHED.
- Integrate modules via Domain Events (`ApprovalRequestedEvent` and `ApprovalCompletedEvent`) using Spring ApplicationEventPublisher.
- Implement end-to-end integration test (`NewsApprovalIntegrationTest`) verifying the full decoupled lifecycle.
- Add Flyway migrations: V28 (Common News Table) and V29 (Generic Approval Tables).
- Standardize technical fields (id, version, audit) across DDD modules via the new Metadata Object pattern.
- Update architectural documentation and roadmap for the Approval System and BaseModel patterns.

fix(build): ensure MapStruct annotation processing and eliminate unrecognized compiler options warning

- Set maven-compiler-plugin version (3.10.1), configure <release>${java.version}</release> and enable forked compilation to ensure proper annotation processing on Java 21.
- Keep mapstruct-processor and lombok-mapstruct-binding on annotationProcessorPaths so MapStruct generation and Lombok binding run during compilation.
- Revert explicit lombok.version override; use Spring Boot parent-managed Lombok to avoid Javac API mismatch.
- Register generated-sources directory so IDEs pick up MapStruct-generated sources.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
