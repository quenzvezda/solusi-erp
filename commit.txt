refactor(arch): complete roadmap phases 4-7; add security tests and smoke verification

Phase 4 - Inventory Decoupling (finalized):
- Verified StockAdjustmentFormIntegrationTest + StockAdjustmentListIntegrationTest already
  cover all important inventory form flows (4 tests each)
- Stock adjustment, stock card, and valuation remain consistent after all refactoring
- inventory module confirmed zero master.model.* imports

Phase 5 - Security Module Decoupling (complete):
- Add PartyReference record (id, code, name) to master.party.application.dto
  as a lightweight cross-module DTO
- Add GetPartyReferenceUseCase + Impl to master.party application layer
- Wire GetPartyReferenceUseCase bean in PartyConfig
- Replace GetPartyEditViewUseCase + Party domain model import in
  UserServiceImpl with GetPartyReferenceUseCase + PartyReference;
  security module no longer imports master.party.domain.model.Party
- Add UserServiceImplTest with 13 unit tests covering:
  * enrichPartyInfo: enrich with party data, null partyId, party not found
  * applyPartyReference: create with party, create without party, password validation
  * Party immutability on update: throws on change, allows first-time set
  * Profile: getProfile enriches party info, password change, confirm mismatch

Phase 6 - Core Module Simplification:
- Verified core module has zero master/inventory imports (clean)

Phase 7 - Hardening and Regression Coverage:
- All major features already have form/list integration tests (inventory + master)
- Smoke tested: taxes list, taxes sorting, parties list, geographics form,
  geographic province autocomplete, facilities list, stock adjustments list
- Zero console errors across all tested pages
- Autocomplete endpoint /api/lookup/geographics/provinces returns correct
  LookupDto payload (id, name, subText, payload)
- mvn clean test: all tests pass

Roadmap: all phases 1-7 items marked [x] complete.
